<div class="section grey white-text center readable">
  <div class="container">

    <div class="pure-g">

      <div class="pure-u-1">
        <p>
          {{profile.description}}
        </p>
      </div>

      <div class="pure-u-1">
        <a class="light-blue-text" href="{{path('index')}}">Home</a> ::
        <a class="light-blue-text" href="{{path('list_articles')}}">Blog</a>
        {% if profile.about is not null  %} :: <a class="light-blue-text" href="{{path('about')}}">About</a> {% endif %}
      </div>
    </div>
  </div>

</div>
