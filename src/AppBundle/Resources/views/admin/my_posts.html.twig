{% extends 'AppBundle:admin:admin_base.html.twig' %}

{% block content %}

  {% for post in posts %}
      <div class="row">
        {{post.content}}

        TAGS:
        {% for tag in post.tags %}
          {{tag.name}}
        {% endfor %}
      </div>
  {% endfor %}

{% endblock content %}
